class MyAppState extends AppState {
    ; Example helper properties for state values
    ; InstallDir {
    ;     get => this.State.Has("InstallDir") ? this.State["InstallDir"] : ""
    ;     set => this.State["InstallDir"] := value
    ; }

    ; Libraries {
    ;     get => this.State.Has("Libraries") ? this.State["Libraries"] : this.State["Libraries"] := Map()
    ;     set => this.SetLibraries(value)
    ; }
}
